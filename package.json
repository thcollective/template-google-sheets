{
  "name": "template-google-sheets",
  "version": "1.0.0",
  "scripts": {
    "local": "sst start --stage local",
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "pretty": "prettier --write \"./**/*.{js,jsx,json}\""
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@sentry/serverless": "^6.18.2",
    "@serverless-stack/cli": "0.66.1",
    "@serverless-stack/resources": "0.66.1",
    "aws-cdk-lib": "2.7.0",
    "dotenv": "^16.0.0",
    "google-spreadsheet": "^3.2.0",
    "googleapis": "^39.2.0",
    "lambda-tester": "^4.0.1"
  },
  "devDependencies": {
    "axios": "^0.26.1",
    "prettier": "^2.5.1"
  }
}
